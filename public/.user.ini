; PHP per-directory configuration for RMA file uploads
; This file is read by PHP (CGI/FPM) from the public/ directory.
; Raise these values to allow attachments up to 10MB.

upload_max_filesize = 10M
post_max_size = 20M
max_file_uploads = 10
memory_limit = 256M
